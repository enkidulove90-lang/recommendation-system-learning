# 创新性分析引擎附加依赖（部署层）
# 基础依赖见项目根 requirements.txt（已含 requests/pydantic/openai 等）

# 表示引擎: SPECTER2 / MiniLM
sentence-transformers>=2.3.0
scikit-learn>=1.3.0
numpy>=1.24.0
# 向量检索（可选，缺省自动回退 numpy/linear）
faiss-cpu>=1.7.4

# REST 服务
fastapi>=0.110.0
uvicorn>=0.27.0
gunicorn>=21.2.0

# 图谱查询（OpenAlex/S2 已用 requests；GROBID 走独立容器）
PyYAML>=6.0.0
python-dotenv>=1.0.0
