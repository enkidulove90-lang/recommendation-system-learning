# Environment variables (contains secrets)
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Node dependencies
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Output
output/
*.json
!package.json
!.mcp.json
!data/profiles/*.json
!data/evidence/*.json
!data/reviews/*.json
!data/registry/*.json
!data/relations/*.json
!data/learning_paths/*.json
!data/metadata/papers/*.json
!data/metadata/recommendations/*.json

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Redbook CLI (installed via pip, cloned for reference only)
redbook/xhs-cli/
redbook/mcp/*.exe

# ===== Experiment large artifacts (cannot push to GitHub / regenerable) =====
# LightGCNpp saved embeddings (~280MB, regenerated at runtime)
baseline/LightGCNpp/code/embs/

# Large multimodal feature arrays & cached adjacency
# (regenerate via prep_amazon_sports.py / fetch_mm_data.py)
*.npy
*.npz

# MixRAGRec: external reference clone (re-clone from upstream; heavy data excluded)
baseline/MixRAGRec/

# ===== Model weights / checkpoints (regenerable, exceed GitHub 100MB limit) =====
# PRISM E15/E14 experiment checkpoints (~4.6GB of .pt)
baseline/PRISM/outputs/
# PRISM 数据副本中的 .pt 权重（数据集 txt 保留）
baseline/PRISM/data/amazon-baby-mmssl/*.pt
# 通用权重扩展名（实验训练产物，不入库）
*.pt
*.pth
*.ckpt
*.bin

# ===== 本地运行时状态 / 一次性审计产物（非工作成果）=====
_serve_live/
_serve_live.log
_serve_pq.json
_serve_st.json
.audit_guizang/
.audit_htmlanything/
.audit_xiaowan/
time.log

# WorkBuddy 本地工作记忆 (个人日志, 不入库)
.workbuddy/
